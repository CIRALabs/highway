<%= form_for(voucher_request) do |f| %>
  <% if voucher_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(voucher_request.errors.count, "error") %> prohibited this voucher_request from being saved:</h2>

      <ul>
      <% voucher_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

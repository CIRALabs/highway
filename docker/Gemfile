source 'http://rubygems.org'


# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '~> 5.2.0'

# Use postgresql as the database for Active Record
gem 'pg', '~> 0.15'
#gem 'sqlite3'

# Use jquery as the JavaScript library
gem 'jquery-rails'
gem 'log4r'

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'

# used by IP address management in ANIMA ACP
gem 'ipaddress'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'

# raven for sentry.io
gem "sentry-raven"

gem 'openssl', :path => '/app/minerva/ruby-openssl'
gem 'sass'
gem 'sassc', "~> 2.3.0"    # because compiled extension
gem 'sass-rails'

# for github warnings
gem 'warden', '~> 1.2.3'
gem 'rake', '~> 12.3.3'
gem "devise", ">= 4.7.1"
gem "nokogiri", ">= 1.10.4"
gem "netaddr", ">= 2.0.4"
gem "actionview", ">= 5.2.4.2"

# use this to get full decoding of HTTP Accept: headers, to be able to
# split off smime-type=voucher in pkcs7-mime, and other parameters
gem 'http-accept'

# for LetsEncrypt
gem 'acme-client'
gem 'dns-update', :path => '/app/minerva/dns-update'

# for static files
gem 'rails-static-router'

# used to generate multipart bodies
gem 'multipart_body', :path => '/app/minerva/multipart_body'
gem 'ecdsa',   :path => '/app/minerva/ruby_ecdsa'
gem 'chariwt', :path => '/app/minerva/chariwt'
gem 'jwt'

gem 'thin'

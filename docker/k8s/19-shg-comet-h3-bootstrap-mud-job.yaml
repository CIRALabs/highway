apiVersion: batch/v1
kind: Job
metadata:
  name: shg-comet-h2-bootstrap-mud
  namespace: shg
spec:
  template:
    spec:
      containers:
      - image: shg-comet:latest
        name: shg-comet
        command: ["bundle", "exec", "rake", "highway:h3_bootstrap_mud"]
        resources: 
          requests:
            memory: 128Mi
            cpu: 100m
      restartPolicy: Never

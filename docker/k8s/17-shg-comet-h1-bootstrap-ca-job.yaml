apiVersion: batch/v1
kind: Job
metadata:
  name: shg-comet-h1-bootstrap-ca
  namespace: shg
spec:
  template:
    spec:
      containers:
      - image: shg-comet:latest
        name: shg-comet
        command: ["bundle", "exec", "rake", "highway:h1_bootstrap_ca"]
        resources: 
          requests:
            memory: 128Mi
            cpu: 100m
      restartPolicy: Never
